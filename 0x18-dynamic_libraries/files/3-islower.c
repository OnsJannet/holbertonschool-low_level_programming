#include "holberton.h"
/**
*_islower-is a function that checks for lowercase character
*@c: The number to be checked
 * Return: Always 0.
*/
int _islower(int c)
{
if (c >= 'a' && c <= 'z')
{
return (1);
}
else
{
}
return (0);
}
