#include <stdio.h>
/**
 *main - main function
 *Return: return value zero (success)
*/
int main(void)
{
int character;
for (character = '0'; character <= '9'; character++)
putchar(character);
putchar('\n');
return (0);
}
