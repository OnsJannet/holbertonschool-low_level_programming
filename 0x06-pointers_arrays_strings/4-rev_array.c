#include "holberton.h"
/**
*reverse_array- is a function that reverses array of integers.
* @*a: int.
* @n: int.
* @a: int.
*
*Return: an integer
*
**/
void reverse_array(int *a, int n)
{
int start;
int temp;
if (start >= n)
{
return;
int temp = start;
int start = n;
int n = temp;
reverse_array(*a, start + 1, n - 1)
}
}
