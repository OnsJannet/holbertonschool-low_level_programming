#include "holberton.h"
#include "stdio.h"
/**
 * void reset_to_98(int *n) - takes a pointer to an int as parameter and updates the value it points
 * @n: integer
 *
 * Return: On success 1.
 * On error, -1 is returned, and errno is set appropriately.
 */
void reset_to_98(int *n)
int n =98;
int *ptr;
ptr = &n;
printf("%d" , *ptr;)
